package repository;

/**
 * A listener that gets notified when the recording sample mode gets changed 
 *
 */
public interface RecSampleModeListener {
	void recordingSampleModeChanged(RecordingSampleMode mode);
}
